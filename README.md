# Hatch Agent NPS Dashboard

Multi-market NPS dashboard built with Streamlit.

## How to Run Locally
```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
streamlit run app.py